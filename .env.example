# Docker Compose (banco dentro do container)
POSTGRES_USER=billwatch
POSTGRES_PASSWORD=

# Backend rodando fora do Docker contra um Postgres local
DB_USER=postgres
DB_PASSWORD=

# Chave HS256 em base64: gere com `openssl rand -base64 48`
JWT_SECRET=
